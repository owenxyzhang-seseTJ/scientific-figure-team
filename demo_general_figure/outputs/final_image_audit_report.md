Final image audit: PASS

Visible rendering issues:
- None detected in the generic figure demo.

Readability and layout check:
- Visible text is readable and no label, tick, legend, or annotation is clipped.
- No gridlines are present by default.
- The note text is unboxed.
- Legend frame is disabled.
- The filled region uses a subtle clipped gradient rather than a flat solid color block.

Font check:
- Arial is available on this machine and is used as the single visible font family in the generated SVG file.
- Normal text and numbers use the same Arial family.

Files inspected:
- `figure_demo.svg`
- `figure_demo.png`
